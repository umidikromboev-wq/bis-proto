# BIS · журнал работ

Обратная хронология: свежее сверху. Формат: дата → сделано → решения → грабли → осталось.
Состояние проекта — `HANDOFF.md`, регламент SEO-переезда — `MIGRATION.md`.

---

## 2026-08-26 — влиты PR #5 и #6, описания кейсов в норму, main закрыт от прямых пушей

### Сделано
- PR #5 `blog` от Бахтиёра проверен и влит (`c5dc47c`), следом PR #6 (`9ac7348`).
  На проде четыре новые статьи: `/post/raznica-mezhdu-crm-i-erp-sistemami`,
  `/post/kak-vybrat-erp-sistemu-dlya-avtomatizatsii-biznesa` и узбекские версии.
- Поверх его ветки — коммит `1081423`: три description, о которых просил 24.08,
  так и остались длинными (neoline RU 229, neoline UZ 285, krupasnab RU 195).
  Переписал их и ещё шесть; все десять `intro` теперь 132–154 символа.
- `1081423` также сжал картинки: `crm-va-erp` 817K→88K, `erp-tizimini-tanlash`
  815K→80K, `Neoline.webp` 458K→8K (sharp, quality 78).
- PR #6 (`e57a562`) убрал дублирование бренда: в четырёх новых `metaTitle` был
  вписан `| BIS-PRO`, а шаблон `layout.tsx` добавляет `| BIS` — на проде выходило
  `… | BIS-PRO | BIS` и заголовок перерастал 60 символов. Теперь `<title>` 49–55.
- На `main` включена branch protection: только через PR, без force-push и удаления,
  админ обходит. Причина — 22.08 в main прилетели три прямых коммита
  `abduraufrasulboyev` с сообщениями «d», «done», «done».

### Решения
- Правки снова положил в ветку Бахтиёра, а не отдельным PR: иначе на прод сразу
  уехали бы «Читайте кейс!» в теле страниц кейсов.
- `intro` не трогал как SEO-поле в отрыве от текста — оно рендерится и как абзац
  на странице кейса, и как meta description. Бахтиёр переписал его под сниппет и
  добавил призывы («Читайте кейс!», «Natijalar bilan tanishing!»), которые в теле
  страницы читаются как мусор. Убрал, смысл сохранил.
- `Neoline.webp` оставил в репозитории, но не подвязывал: тип `Img` в
  `src/content/types.ts` вообще не имеет поля `src`, у кейсов только `brief` —
  ТЗ дизайнеру. Рендеринг фото кейсов пришлось бы добавлять отдельно.
- `required_status_checks` намеренно не включал: проверка `preview` постоянно
  красная, она бы заблокировала любой мерж.

### Грабли
- **`Neoline.webp` — это логотип на белом фоне, а не запрошенный кадр.** В `img.brief`
  просили «сотрудник со смартфоном на фоне стеллажей». Сжатие 458K→8K выглядело
  ошибкой, пока не открыл картинку: для плашки с логотипом 8K — норма. Вопрос
  про фото для кейсов Neoline и «Крупаснаб» так и не закрыт.
- **`preview.yml` падает за 7 секунд на каждом пуше с 13 августа** — секрет
  `VERCEL_TOKEN` не задан. При этом Vercel давно создаёт превью сам, в том числе
  на коммиты Бахтиёра (`d2d0cd5`, `1081423`). Workflow — мёртвый обход, но удалить
  не дал защитный хук; нужно прямое согласие.
- Отступы в `blocks.ts` и `uz/blocks.ts` поехали третий раз подряд (2–3 пробела
  вместо 4, закрывающая `],` без отступа). Одним замечанием это не лечится —
  нужен prettier в PostToolUse или pre-commit.

### Осталось
- [ ] Решить судьбу `preview.yml`: удалить или завести секрет `VERCEL_TOKEN`.
- [ ] Обложки статей идут с `alt=""` — у всех 18 статей, не только у новых.
      Это шаблон, а не контент; править вместе с текстами alt.
- [ ] Логотип `bis-mark-white.png` в одном месте без `alt`.
- [ ] Фото для кейсов Neoline и «Крупаснаб» — запросить у клиента.
- [ ] Настроить prettier на пре-коммит, чтобы отступы не всплывали каждый раз.

---

## 2026-08-20 — влит PR #4 (кейсы Neoline и «Крупаснаб»), починена цитата на странице кейса

### Сделано
- PR #4 `portfolio` от Бахтиёра разобран и влит в main (`71ded98`), задеплоен
  на www.bis-pro.com. Два новых кейса: `/portfolio/neoline`, `/portfolio/krupasnab`
  и узбекские версии. Всего кейсов стало пять.
- Контент чистый: `tsc --noEmit` и `next build` без ошибок, индексы спреда
  `ru.cases[3]` и `ru.cases[4]` совпадают с новыми записями, сдвига по горизонтали
  нет на 320/768/1440, ошибок в консоли нет.
- Поверх ветки — коммит `8ebfcb7`: цитата на странице кейса рендерилась шириной
  185px вместо 801px и рассыпалась по одному слову в строку.

### Решения
- Правку положил в ветку Бахтиёра, а не отдельным PR в main: иначе мерж выкатил бы
  сломанную цитату сразу на трёх страницах, а чинили бы её вторым деплоем.
- Отступы у новых записей выровнены и возвращён перевод строки в конце
  `blocks.ts` и `uz/blocks.ts` — то же замечание было к прошлым веткам.

### Грабли
- **`.d-quote` — сетка `clamp(9rem,15vw,14rem) minmax(0,1fr)`: узкая колонка под фото,
  широкая под текст.** Она верна для `proof.tsx`, где фото есть. В `CaseView` фото нет,
  `blockquote` оставался единственным ребёнком и падал в колонку под фото.
  Баг лежал на проде на `/portfolio/brauf` с самого начала, просто там одна цитата
  и её не заметили. Лечение — `.d-quote:has(.d-quote-photo)`, блок на главной не тронут.
  Тот же класс, что и с `.di-post-card` в PR #3: сетка на переменное число детей.
- Проверять такие вещи замером `getBoundingClientRect`, а не глазами по скриншоту:
  на fullPage-снимке блок был скрыт `Reveal`-анимацией и выглядел просто пустым местом.

### Осталось
- [ ] В репозитории не заведён секрет `VERCEL_TOKEN` — GitHub Action «Preview deploy»
      падает на каждом PR. Код тут ни при чём, но красный чек приучает его игнорировать.
- [ ] У кейсов Neoline и «Крупаснаб» нет фотографий, в `img.brief` лежит только описание кадра.
- [ ] У «Крупаснаб» узбекская версия `before` разошлась с русской по смыслу
      четвёртого пункта — сверить с Бахтиёром, какая версия верна.
- [ ] Цитаты в новых кейсах написаны от третьего лица («руководители отметили…»),
      а стоят в блоке прямой речи. Попросить у клиента настоящую цитату.

## 2026-08-15 — влит PR #3 (мобильный блог), проставлены alt на обложках

### Сделано
- PR #3 `blogres` от Бахтиёра (открыт 14 августа) разобран и влит в main, задеплоен
  на www.bis-pro.com. Коммит `b765f96` сохранён, поверх — зачистка `87b5b10`.
- Фикс верный: `.di-post-card:has(.di-post-cover)` задаёт три колонки, а мобильное
  правило на строке 187 бьёт только `.di-post-card` — специфичность (0,1,0) против
  (0,2,0), поэтому карточки с обложкой на телефоне оставались трёхколоночными.
  Ветка добавляет `@media (max-width: 700px)` и `420px` с тем же `:has()`.
- Из ветки убран дубль блока `@media (max-width: 640px)` (копия строк 220–222)
  и продублированный комментарий; добавлен перевод строки в конце файла.
- Обложки блога отдавались с `alt=""` — 7 контентных картинок внутри ссылок.
  Подставлен заголовок поста (`inner-views.tsx`, список блога и «читайте также»).
- Приёмка `/blog` после деплоя: 320/768/1440 чисто, картинок без alt 8/10 → 1/10.

### Решения
- Мерж без squash: авторство коммита Бахтиёра сохранено, зачистка отдельным коммитом.
- Оставшийся 1/10 без alt — белая копия знака с `aria-hidden="true"`, пустой alt
  у неё намеренный. Не трогали.

### Грабли
- `Deploy to production` в Actions зелёный через 3 секунды — workflow только дёргает
  Vercel Deploy Hook, сборка идёт на Vercel асинхронно ещё ~25 секунд. Зелёный
  Actions ≠ правки на проде: проверять надо отданный CSS или `vercel ls`.
- Превью-деплой на ветке `Baxtiyor` снова упал — та же блокировка Vercel по автору.

### Осталось
- [ ] Title страницы `/blog` 72 знака, Google режет около 60. Решение за Ойбеком —
      как и title главной.
- [ ] `reveal.tsx:30` — lint-ошибка «setState synchronously within an effect».
      Досталась от каркаса дизайн-версии (`9863f01`), сборку не ломает.
- [ ] Удалить мёртвые PNG: `bis-logo1`, `bis-logo-white2`, `bis-logo3`, `bis-white-logo`,
      `bis-white-logoo` — 2.3 МБ, ни один не используется.

---

## 2026-08-12 — влит PR #1 (логотип и блог), починены две 404

### Сделано
- PR #1 от Бахтиёра висел с 3 августа без ревью — из-за этого встала работа всей команды.
  Разобран, дополнен коммитом `d203878`, влит в main, задеплоен на www.bis-pro.com.
- Знак BIS вырезан из холста 9000×9000 (сам знак занимал 24% высоты) →
  `bis-mark.png` / `bis-mark-white.png`, 1200×408, 70 и 33 КБ вместо 1.3 МБ и 733 КБ.
  Высота в `menu.css` вернулась к нормальной шкале, знак встал на сетку страницы.
- `json-ld.tsx` ссылался на `/design/bis-logo.png`, переименованный в ветке → логотип
  Organization отдавал 404. Переподключён к `bis-mark.png`.
- Метаданные объявляли `/icon-192.png`, файла в `public/` не было → 404 на каждой странице.
  Сгенерирован из `src/app/icon.png`.
- `sap-business-one-ombor-nazorati`: `metaTitle` и `excerpt` были целиком по-русски.
  Переведены. Русского в uz-контенте не осталось (проверено скриптом по всем полям).

### Решения
- Ветка Бахтиёра дополнена, а не переписана: авторство его коммитов сохранено.
- Замечание автоприёмки «94/139 картинок без alt» — ложное: 52 это `aria-hidden` иконки,
  42 — вторые копии бегущих лент (логотипы клиентов, команда), у них `alt=""` намеренно.
  Первая копия каждой ленты alt имеет. Не трогали.

### Грабли
- Роль `maintain` на GitHub недоступна в репозиториях личного аккаунта — только
  read / write / admin. Абдурауфу оставлен write.
- Превью-деплой Vercel на PR по-прежнему BLOCKED: автор коммита не в команде Vercel.
  Прод собирается через GitHub Actions и не зависит от этого, но превью на PR не работают.

### Осталось
- [ ] Удалить мёртвые PNG: `bis-logo1`, `bis-logo-white2`, `bis-logo3`, `bis-white-logo`,
      `bis-white-logoo` — 2.3 МБ, ни один не используется.
- [ ] Title главной 70 знаков, Google режет около 60. Решение за Ойбеком.
- [ ] Разобрать в `inner.css` запасные цвета вида `var(--text, #2b2b2b)` и
      `.di-article { display:block; opacity:1; visibility:visible }` — лечат симптом.

---

## 2026-08-04 — мобильная вёрстка: убран сдвиг вбок и текст лесенкой

### Сделано
- Аудит Playwright на 320 / 375 / 414 px, 12 страниц в двух локалях (было: 3 страницы со
  сдвигом вбок, на всех внутренних срезало кнопку меню). После правок — 36 из 36 чисто.
- `/vacancy`: дорожка найма стояла на инлайновом `gridTemplateColumns: repeat(4, …)` —
  инлайн бьёт медиазапросы, на 320 px колонки были по 52 px и текст вставал лесенкой.
  Заменено на класс `.sc-road-4` (schemes.css), сворачивается в 3 и 1 колонку.
- `/simulator`: `.wc-insights` с `minmax(320px, 1fr)` шире контейнера с полями на экране
  320 px → страница уезжала на 20 px. Теперь `minmax(min(320px, 100%), 1fr)`.
- Шапка: `.dh-phone` прятался только на главной (правило жило в `hero.css`), на внутренних
  страницах логотип + номер + кнопка не помещались → прячем до 540 px, уплотняем до 380 px.
- Заголовкам добавлен `overflow-wrap: break-word` — длинные узбекские слова
  («Superfoydalanuvchilarni») выпирали за карточку на /courses.

### Решения
- Число колонок дорожки задаётся классом, а не инлайном: инлайн невозможно перебить
  медиазапросом без `!important`.
- `min(320px, 100%)` вместо правки брейкпоинта: работает на любой ширине контейнера.

### Грабли
- `body { overflow-x: hidden }` маскировал переполнение: страница «не скроллилась», но
  контент по краям резался. Мерить надо `scrollWidth` дочерних, а не факт скролла.
- Локальный `next start` держал старый порт после пересборки и отдавал 500 на новый
  CSS-чанк — замер показал «сломанную» вёрстку. Убивать процесс через `lsof -ti:PORT`.
- `main` уехал вперёд (коммит `bed1b4f` от программиста) — перед пушем rebase.

### Осталось
- [ ] Прогнать тот же аудит на страницах статей блога и портфолио-кейсах
- [ ] Пункт из Trello: логотип, разъехавшийся узбекский в статьях, кейсы Krupasnab и Neoline

---

## 2026-08-03 — правки программиста перенесены в рабочую ветку

### Сделано
- Разобраны два коммита `abduraufrasulboyev` от 01.08 (`f7d6e4a`, `97a40a5`). Они лежали
  в `main` — ветке, которая отстаёт от `design` на 10 коммитов, поэтому на прод не попадали.
- Его SEO-тексты заведены в `src/content/seo.ts`: новое описание главной (RU и UZ) и
  ключевые слова. У узбекского исправлены опечатка `Loihangiz` → `Loyihangiz` и прямые
  апострофы → `oʻ/gʻ`.
- `pageMetadata()` теперь отдаёт `keywords` (если заведены в справочнике) и картинку
  для соцсетей по умолчанию — свою на каждый язык.
- Добавлены OG-картинки `public/og/bis-ru.png` и `bis-uz.png` (1200×630, лого на
  фирменном градиенте). Раньше их не было вовсе — ссылки расшаривались без превью.
- Из его правок взяты: переменная шрифта `--font-manrope`, `themeColor`, прижатый
  подвал (`flex min-h-full flex-col` + `flex-1` на `<main>`).

### Решения
- **Заголовки страниц не тронуты.** Они перенесены с прежнего сайта дословно, по ним
  набирались позиции. Шаблон `%s | BIS`, который он добавил, не берём: он приписал бы
  суффикс к каждому проиндексированному title.
- **`«официальный партнёр SAP»` в ключевые слова не возвращён** — статуса нет, SAP его
  не выдаёт, заказчик снял формулировку 22.07.
- **Хардкод `baseUrl` и `robots: index: true` не берём.** Адрес и индексация живут в
  `NEXT_PUBLIC_SITE_URL` / `IS_INDEXABLE`: иначе превью-сборки начнут отдавать canonical
  прода и лезть в выдачу.
- Иконки оставлены прежние: у него полное лого с подписью, оно нечитаемо в 16 px;
  в проекте уже лежит монограмма.
- Удаление 163 строк из `package-lock.json` не переносилось.

### Грабли
- Файловая конвенция `opengraph-image.png` здесь не работает: страницы задают свой
  `openGraph` через `pageMetadata`, и он перекрывает картинку родительского сегмента —
  превью получала только главная. Поэтому картинка подставляется в `pageMetadata`.
- Он сослался на `/photos/dashboard.webp`, а ассеты лежат под `public/design/**`.
  Плюс `og-image-ru.png`, `icon-192.png`, `apple-icon.png`, `public/favicon.ico`,
  на которые он ссылался, в репозитории отсутствуют.

### Осталось
- [ ] Проверить с заказчиком новое описание главной: в нём «Узнайте стоимость проекта!»,
      а цены с сайта убраны 31.07.
- [ ] Сказать программисту: рабочая ветка — та, из которой собирается прод.

---

## 2026-07-31 — подключение домена bis-pro.com

### Сделано
- Домены добавлены в Vercel-проект `bis-proto` (team umid-s-projects):
  `www.bis-pro.com` (основной) + `bis-pro.com` (308 → www). Оба **верифицированы**.
- В Vercel Production заданы env: `NEXT_PUBLIC_SITE_URL=https://www.bis-pro.com`,
  `NEXT_PUBLIC_GA_ID=G-NF74XSJ06L`; прод передеплоен — robots открыт (`Allow: /`),
  canonical/sitemap указывают на www.bis-pro.com, GA-счётчик старого сайта подключён.
- DNS правился в панели Ahost (clients.ahost.uz, аккаунт клиента **AXMADJON, ID U28805**),
  домен оплачен до 13.05.2027. Изменения:
  - `@ A`: 198.202.211.1 (Webflow) → **216.150.1.1** (Vercel)
  - добавлена `@ A` → **216.150.16.1** (вторая рекомендованная Vercel)
  - `www CNAME`: cdn.webflow.com → **fd790bbb5f586751.vercel-dns-016.com**
  - добавлены 2 TXT `_vercel` (vc-domain-verify для обоих хостов)
  - **ничего не удалено** (прямое требование Умида) — все 22 бэкенд-записи клиентов BIS
    и старые `_webflow` TXT на месте. Бэкап исходной зоны: `dns-zone-backup-2026-07-31.md`.

### Ключевые решения и факты
- Домен один — **bis-pro.com** (продлён/перенесён на Ahost), нового не покупали.
  SEO-план (MIGRATION.md) сохраняется без изменений: адреса те же, редиректов не нужно.
- Домен ранее числился в **чужом Vercel-аккаунте** (прошлый разработчик) — поэтому
  потребовалась TXT-верификация; после неё домен «переехал» к нам.
- Неймсерверы оставлены у Ahost (`rdns1/2/3.ahost.uz`) — НЕ переключали на Vercel DNS,
  потому что зона несёт критичную инфраструктуру BIS: ~22 A-записи бэкендов клиентских
  проектов (orient-cer, istyle, exact, brrauf, bobs/rofood-mdokon-integ, credo, icomfort,
  probox, good-life, macintosh, logs, api/api2) на серверах 91.107.233.191,
  185.201.236.210, 5.252.23.162, 88.218.93.119. Терять их нельзя — у клиентов BIS
  встанут кассы/интеграции.

### Грабли (важно для будущих правок DNS у Ahost)
- Вкладка «DNS-хостинг» сперва показывает красный баннер «Исправить» — это ложная
  тревога незагруженного менеджера; после клика зона открылась (та же, что на rdns).
- Зона редактируется в **iframe**; инлайн-правки значений через JS
  (`val().trigger()`) НЕ помечаются как изменённые — сохраняются только правки,
  введённые реальным вводом (клик → выделить → напечатать) + кнопка «Сохранить изменения».
- «Добавить запись» — модалка с select2; поле «Значение» появляется после выбора типа.
- Панель применяет изменения в свою БД сразу, но **rdns-серверы синкают зону с лагом**
  (TXT долетели за минуты, апекс/www — дольше). SOA serial при этом не меняется.
- Разрешение окна браузера менялось между скриншотами → один triple-click попал не в ту
  строку (credo-backend-test получил чужой IP). Замечено и исправлено ДО сохранения.
  Урок: перед кликом в таблицу — свежий скриншот и сверка координат.

### Синхронизация зоны (17:09, ~40 мин после сохранения)
- **apex опубликовался**: bis-pro.com → 216.150.1.1 + 216.150.16.1, отдаёт 308 на www ✅
- **www НЕ опубликовался**: все три NS (rdns1/2/3) продолжают отдавать `cdn.webflow.com`,
  хотя в панели после перезагрузки страницы значение правильное (`fd790bbb…vercel-dns-016.com`).
  Вывод: баг публикации зоны у Ahost — A-записи ушли, изменение CNAME нет.
  **Обход:** тронул TTL записи www (14440 → 3600) и сохранил заново, чтобы форсировать
  перепубликацию. Низкий TTL при переезде и так правильная практика.
- Простоя нет: посетители попадают на старый Webflow (apex → 308 → www → Webflow).

### Домен переключён и проверен (17:30)
TTL-трюк сработал — www опубликовался. Приёмка (все проверки через `--resolve` на Vercel-IP,
чтобы не врал локальный кэш):
- 56/56 адресов sitemap → **200**, ни одного битого
- Легаси-адреса Webflow → **308** на новые: `/form-call`→`/contacts`,
  `/portfolio-page-1..4`→`/portfolio(/brauf)`, `/vacancy/trebuetsya-*`→`/vacancy`, и то же под `/uz`
- Апекс `bis-pro.com` → **308** на `www` ✅ · SSL выпущен ✅ · `robots.txt` открыт ✅
- Отдаёт новый сайт: `server: Vercel`, Next.js, наш CSP-заголовок

**Грабля, стоившая получаса диагностики:** после переключения `curl` с Мака показывал старый
Webflow (`server: cloudflare`, sitemap на 72 адреса вместо 56, `/simulator` 404). Причина —
кэш системного резолвера macOS: старый CNAME имел TTL 14440 (4 ч), `dig` его обходит, а `curl`
использует. Понять это позволил `-w %{remote_ip}`: соединение шло на 198.202.211.1 (старый
Webflow), хотя `dig` уже отдавал Vercel. **Вывод: после смены DNS проверять только через
`curl --resolve <host>:443:<новый IP>`, иначе диагностируешь собственный кэш.**

### Фавикон (17:35)
На сайте стояла **заглушка Next.js** (чёрный круг с треугольником), не бренд. Заменён на
монограмму из логотипа: белая «B» на фирменном графите `#2c2c2f`, скруглённая плитка.
Файлы в `src/app/`: `favicon.ico` (6 размеров 16–256, каждый отрисован отдельно),
`icon.png` 512, `apple-icon.png` 180. Собраны скриптом из `public/design/bis-logo-white.png`.

Две грабли:
- **Целый логотип «BIS» в квадрат не влезает** — при 16px три буквы превращаются в кашу
  (проверено глазами). Правильно — монограмма из одной буквы.
- **PIL не умеет мультиразмерный `.ico`**: `append_images` молча сохраняет только первый
  размер (файл вышел 342 б вместо 10 КБ). Пришлось писать ICONDIR/ICONDIRENTRY вручную —
  зато каждый размер отрисован под себя, а не ресайзом одного мастера.
  Проверка: `file favicon.ico` должен показать «6 icons».

### Убраны все цены (17:50, по решению Умида)
Тарифы больше не показывают деньги — блок переосмыслен как **форматы работы**:
пакеты, сроки и состав работ остались, суммы убраны.
- `blocks.ts` + `uz/blocks.ts`: сняты `price`/`priceNote` у трёх пакетов; кикер
  «Стоимость»→«Форматы работы» / «Narx»→«Ish formatlari»; заголовок и лид переписаны
  с бюджета на объём проекта; удалена служебная пометка «вилки предварительные».
- `types.ts`: поля `price`/`priceNote` и блок `budget` убраны из типов.
- `product-b1.ts`, `product-s4.ts` (+ узбекские): удалены таблицы бюджета целиком
  (лицензии $1 800–3 200, внедрение $18 000–45 000, инфраструктура $25 000–80 000).
- `close.tsx`, `product-page.tsx`: убран рендер цены и таблицы бюджета.

**Попутно исправлен баг:** в карточке пакета срок выводился как `{txt.term}{txt.term}` —
на месте значения печаталась подпись («СрокСрок» вместо «Срок 3–4 месяца»).

**Осознанно оставлено:** «$200K инвестировано в развитие экспертизы» в блоке о компании —
это показатель доверия, а не цена услуги. Убрать — по слову заказчика.

**Найдено попутно:** `navHome` в `blocks.ts` и `uz/blocks.ts` — мёртвый код, его никто
не импортирует (реальное меню берётся из `nav` и `content/nav.ts`). Не удалял.

Проверка на живом домене: цен нет ни на одной странице (RU и UZ), слова «Стоимость»
в разметке 0 вхождений, вёрстка карточек проверена скриншотами на 1440 и 390.

### Осталось
- [ ] Передача клиенту: перенос GitHub-репо и Vercel-проекта, группа BIS для бота заявок
      (сменить `TELEGRAM_CHAT_ID`), доступы GA + Search Console, отправка sitemap в GSC.
- [ ] Смёржить `design` → `main`, чтобы главная ветка не была устаревшей до передачи.
- [ ] Через сутки после переезда — проверить Search Console на ошибки покрытия.
- [ ] Передача клиенту: репозиторий GitHub, Vercel-проект (transfer), группа BIS для
      бота заявок @bis_sap_leads_bot (сменить TELEGRAM_CHAT_ID), доступ GA + Search
      Console (после переключения — отправить sitemap), напомнить долги по контенту
      (цифры тарифов, кейсы PRIME/Avanta, фото команды, 7-й сотрудник, даты статей).

---

## 2026-07-29 — SEO-переезд с Webflow (кратко, детали в MIGRATION.md)

- Решение: сохранить старые адреса вместо редиректов; маршруты переименованы
  (`academy→courses`, `careers→vacancy`, `cases→portfolio`, `sap-s4hana→sap-s-4hana`),
  статьи на `/post/<slug>`. 42 адреса → 200, 14 → 308, 16 черновиков Webflow → 404.
- `title`/`description` старого сайта перенесены в `src/content/seo.ts`;
  карта языковых пар статей — `src/content/post-slugs.ts`.
- До переключения домена требовались env `NEXT_PUBLIC_SITE_URL` + `NEXT_PUBLIC_GA_ID`
  (сделано 31-го, см. выше).

## 2026-07-26 и ранее — дизайн-версия (кратко, детали в HANDOFF.md)

- Ветка `design` — прод; `main` отстал. Деплой вручную: `npx vercel --prod --yes`.
- Дизайн переехал в корень: `/(ru)/*` и `/(uz)/uz/*`.
- Калькулятор оборотного капитала `/simulator` по Google-файлу клиента (сверять цифры).
- Снято «официальный партнёр SAP», убран SAP Business ByDesign (правки заказчика 22.07).
- Playwright-обход всех маршрутов ловил битые картинки (`{fl.after}`, пути `/design/`).
- Заявки: `/api/lead` → бот @bis_sap_leads_bot (пока в личку Умида).
